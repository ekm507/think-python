import numpy as np
import matplotlib.pyplot as plt

a = np.arange(-10, 10, 0.1)
b = np.zeros([10, 10])
c = np.sin(a)
d = np.linspace(0, 10, 100)
e = np.random.random()
f = np.random.choice(a)
print(f)